# ~/.tmuxinator/blog.yml

tmux_command: byobu

name: blog
root: /mnt/unreal/git/joshuacox.github.io/

windows:
  - blog:
        - ls -lh _drafts && wc -l _drafts/*
  - serve:
        - git pull
        - git push
        - jekyll serve --drafts
